# SuperVideoMix 中文使用说明

SuperVideoMix 是一个“先分析、再计划、后执行、最后验证”的授权短视频清理与翻新 Skill。

当前已安装：

- Skill 调用名：`$super-video-mix`
- 安装路径：`/Users/solo/.codex/skills/super-video-mix`
- CLI：`/Users/solo/.codex/skills/super-video-mix/scripts/video_pipeline.py`

Codex 与 Claude 当前均通过符号链接使用同一份仓库源码；升级仓库后会即时生效。若本机还有复制安装的其他 Agent，升级后执行：

```bash
python3 "/Users/solo/Desktop/AI工作室/SuperVideoMix/super-video-mix/scripts/install_skill.py" --sync-agents
```

安装新 Agent 时先用安装器登记目标目录，后续即可被 `--sync-agents` 自动纳入：

```bash
python3 "/Users/solo/Desktop/AI工作室/SuperVideoMix/super-video-mix/scripts/install_skill.py" \
  --target-dir /absolute/path/to/agent/skills
```
- 版本：`0.6.0`

只处理自有或已获授权的视频。镜像、调色、裁切和水印/字幕区域处理不能被表述为规避平台检测或消除版权风险。

## 1. 准备环境

确认 `python3`、`ffmpeg` 和 `ffprobe` 可用：

```bash
python3 --version
ffmpeg -version
ffprobe -version
```

设置 CLI 路径：

```bash
SKILL_DIR="/Users/solo/.codex/skills/super-video-mix"
PIPELINE="$SKILL_DIR/scripts/video_pipeline.py"
```

所有示例都默认：

- `INPUT` 是输入视频路径。
- `OUTPUT.mp4` 是新输出路径，不能与输入相同。
- `analysis.json`、`plan.json`、`execution.json`、`verification.json` 是报告文件。

## 2. 标准工作流

```text
analyze → review → plan → review gate → apply → verify
```

不要跳过 `analyze` 和 `plan`。`analyze`、`dedupe`、`plan`、`verify` 不修改输入视频；只有 `apply` 会创建新视频。

### 2.1 查看帮助

```bash
python3 "$PIPELINE" --help
python3 "$PIPELINE" plan --help
```

需要机器读取时，命令末尾加 `--json`；人类提示写到 stderr，stdout 只输出 JSON。

## 3. 最保守：只分析，不修改

### 3.1 分析媒体与废片尾

```bash
python3 "$PIPELINE" analyze INPUT \
  --source douyin \
  --report analysis.json
```

`--source` 可选：`generic`、`douyin`、`tiktok`、`instagram`、`youtube-short`。

分析报告会包含：媒体信息、尾部扫描采样、黑屏、低细节、静帧/重复、模糊和静音等证据，以及 `junk_tail` 建议区间。

只想探测容器和轨道，不扫描废片尾：

```bash
python3 "$PIPELINE" analyze INPUT \
  --no-tail-analysis \
  --report analysis.json
```

### 3.2 查找完全相同素材

```bash
python3 "$PIPELINE" dedupe "/path/to/video-folder" \
  --report duplicates.json
```

当前 `dedupe` 使用 SHA-256 精确去重，不删除重复文件。需要发现转码、裁切或轻微编辑后的相似素材时，运行基础感知指纹报告：

```bash
python3 "$PIPELINE" fingerprint "/path/to/video-folder" \
  --threshold 0.86 --report perceptual-dedupe.json
```

报告包含跨时间采样的感知指纹、`likely/possible/distinct` 候选和候选组。它只用于本地整理与人工复核，不修改内容、不删除源文件，也不代表任何平台的重复判定。重编码或翻新后应重新运行 `fingerprint`；SHA-256 会变化，而感知相似度可帮助关联新旧素材。

如需清理容器中的非内容元数据（源文件不覆盖），先完成编码兼容化，再执行：

```bash
python3 "$PIPELINE" metadata INPUT.mp4 \
  --output "INPUT__catalog-clean.mp4" --report metadata.json
```

该步骤移除格式/流标签和章节，保留视频、音频内容并验证为可播放的 H.264 + AAC MP4。清理后必须重新生成 `fingerprint` 报告。

## 4. 下载后 VP9/VP09 兼容化

部分下载器会保存为 VP9（`vp09`）视频 + AAC 音频。QuickTime 对 VP9 视频流不兼容，表现为只有声音、没有画面。所有下载视频进入查重、分析或翻新前，先执行：

```bash
python3 "$PIPELINE" normalize DOWNLOADED.mp4 \
  --output "DOWNLOADED__h264-aac.mp4" \
  --report normalize.json
```

该命令会检测 `video_codec`、`audio_codec` 和容器：VP9/VP09、非 H.264、非 AAC 或非 MP4 时使用 `libx264 + yuv420p + AAC` 转码；已兼容文件则安全 remux。输出始终是新 `.mp4`，源文件不覆盖。确认报告中的 `verified=true` 后，再把新输出交给 `dedupe`、`analyze` 或 `plan`。

## 5. 清理废片尾

### 4.1 分析后接受建议

先查看 `analysis.json`，确认尾段确实是黑屏、静音或无效空镜，再生成 trim plan：

```bash
python3 "$PIPELINE" plan INPUT \
  --analysis analysis.json \
  --accept-suggested-tail \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

`--accept-suggested-tail` 是明确审批，不会自动接受分析器建议。

### 4.2 手动指定保留时长

适用于你已经明确知道有效内容结束时间的情况：

```bash
python3 "$PIPELINE" plan INPUT \
  --trim-end 27.8 \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

`--accept-suggested-tail` 和 `--trim-end` 不能同时使用。

### 4.3 执行并验证

```bash
python3 "$PIPELINE" apply plan.json \
  --report execution.json

python3 "$PIPELINE" verify plan.json \
  --report verification.json
```

执行器会：

1. 再次检查输入 SHA-256。
2. 使用 `trim/atrim` 同步裁切视频和音频并重置 PTS。
3. 写入同目录临时文件。
4. 检查可完整解码、轨道、分辨率、时长和音视频时长差。
5. 验证通过后无覆盖交付到 `OUTPUT.mp4`。

## 6. 保真输出与构图

### 5.1 原样保真

所有增强关闭，不拉伸、不镜像、不变速：

```bash
python3 "$PIPELINE" plan INPUT \
  --preset preserve \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

### 5.2 `fit`：完整保留画面，补边

适合横屏视频转 Shorts/Reels，同时不裁掉画面：

```bash
python3 "$PIPELINE" plan INPUT \
  --composition fit \
  --resolution 1080x1920 \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

### 5.3 `fill`：铺满画布，允许裁切

适合主体位于画面中心、可以接受边缘裁切的素材：

```bash
python3 "$PIPELINE" plan INPUT \
  --composition fill \
  --resolution 1080x1920 \
  --approve-preview \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

### 5.4 `stretch`：非等比拉伸

只有用户明确接受变形时才使用：

```bash
python3 "$PIPELINE" plan INPUT \
  --composition stretch \
  --resolution 1080x1920 \
  --approve-high-risk \
  --approve-preview \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

用户只说“转成 9:16”时，优先选择 `fit`、`fill` 或后续的 `smart`，不要默认 `stretch`。

## 7. 镜像与反转

### 6.1 水平镜像

`--mirror` 是 `--flip horizontal` 的快捷写法：

```bash
python3 "$PIPELINE" plan INPUT \
  --mirror \
  --approve-preview \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

### 6.2 垂直反转或双向反转

垂直反转和双向反转是 high risk：

```bash
python3 "$PIPELINE" plan INPUT \
  --flip vertical \
  --approve-high-risk \
  --approve-preview \
  --final-output OUTPUT.mp4 \
  --output plan.json

python3 "$PIPELINE" plan INPUT \
  --flip both \
  --approve-high-risk \
  --approve-preview \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

`both` 使用 `hflip,vflip`，视觉上等价于 180° 旋转，不等同于单纯水平镜像。不得为了“去重”自动镜像。

## 8. 调色、滤镜、降噪、锐化

### 7.1 只调色

可选预设：`natural`、`warm`、`vivid`。

```bash
python3 "$PIPELINE" plan INPUT \
  --color natural \
  --approve-preview \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

### 7.2 只加滤镜

可选预设：`cinematic`、`soft`、`vintage`、`monochrome`。

```bash
python3 "$PIPELINE" plan INPUT \
  --filter cinematic \
  --approve-preview \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

### 7.3 降噪后锐化

推荐顺序是先降噪，再锐化：

```bash
python3 "$PIPELINE" plan INPUT \
  --denoise light \
  --sharpen light \
  --approve-preview \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

可选强度：`light`、`medium`。`medium` 更容易损失纹理或产生边缘光晕。

### 7.4 调色和滤镜同时使用

两者可能重复影响对比度、饱和度和色温，因此必须显式接受冲突检查：

```bash
python3 "$PIPELINE" plan INPUT \
  --color warm \
  --filter cinematic \
  --approve-preview \
  --approve-conflicts \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

未加入 `--approve-conflicts` 时，`apply` 会拒绝执行。

## 9. 音画同步变速

### 8.1 加速

```bash
python3 "$PIPELINE" plan INPUT \
  --speed 1.06 \
  --approve-preview \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

### 8.2 慢放

```bash
python3 "$PIPELINE" plan INPUT \
  --speed 0.8 \
  --approve-preview \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

视频使用 `setpts`，音频使用 `atempo` 链；`0.25–4.0` 范围内会自动拆分合法的 `atempo` 因子。执行后必须检查 `av_sync_duration`。

## 10. 固定水印或硬字幕区域

只对已获授权的固定区域使用。区域格式为 `X:Y:W:H`，支持像素和百分比。

### 先识别文字标签和 Logo 位置

位置不固定时，先生成二维候选框、最小损失裁剪矩形和预览图：

```bash
python3 "$SKILL_DIR/scripts/smart_label_detect.py" INPUT.mp4 \
  --creator CREATOR_NAME --memory MEMORY_DIR \
  --report label-crop-analysis.json \
  --preview-dir previews
```

红框为候选文字/Logo，绿框为建议保留区域。`review_required=true`、内容损失超过 20%、候选框覆盖主体或候选不贴边时不得直接执行。只有人工确认后才允许加入 `--confirm-memory` 写入创作者记忆。

批量分析使用：

```bash
python3 "$SKILL_DIR/scripts/smart_label_detect.py" --input-dir INPUT_DIR \
  --creator CREATOR_NAME --memory MEMORY_DIR \
  --output-dir ANALYSIS_DIR --preview-dir ANALYSIS_DIR/previews
```

该命令仅生成 `label_crop_analysis.json`、`label_crop_screening.tsv` 和预览图，不修改源视频。

### 9.1 固定小区域

```bash
python3 "$PIPELINE" plan INPUT \
  --remove-region '78%:78%:20%:18%' \
  --confirm-authorized-removal \
  --approve-preview \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

### 9.2 底部硬字幕带

```bash
python3 "$PIPELINE" plan INPUT \
  --subtitle-band '0.72:0.94' \
  --confirm-authorized-removal \
  --approve-preview \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

### 9.3 多区域

```bash
python3 "$PIPELINE" plan INPUT \
  --remove-region '78%:78%:20%:10%' \
  --remove-region '4%:4%:14%:8%' \
  --confirm-authorized-removal \
  --approve-preview \
  --approve-high-risk \
  --final-output OUTPUT.mp4 \
  --output plan.json
```

多区域或大区域会升级为 high risk。移动水印、复杂运动背景、大面积字幕不要反复堆叠 `delogo`；当前 Skill 不内置 VSR，需要后续 adapter。

## 11. 常用完整搭配

### 10.1 抖音：清理片尾 + 转 9:16 + 保留画面 + 轻增强

```bash
python3 "$PIPELINE" analyze INPUT \
  --source douyin --report analysis.json

python3 "$PIPELINE" plan INPUT \
  --analysis analysis.json \
  --accept-suggested-tail \
  --composition fit --resolution 1080x1920 \
  --denoise light --sharpen light \
  --approve-preview \
  --final-output OUTPUT.mp4 --output plan.json

python3 "$PIPELINE" apply plan.json --report execution.json
python3 "$PIPELINE" verify plan.json --report verification.json
```

### 10.2 横屏素材：转 Shorts + 水平镜像 + 调色

```bash
python3 "$PIPELINE" plan INPUT \
  --composition fit --resolution 1080x1920 \
  --mirror --color natural \
  --approve-preview \
  --final-output OUTPUT.mp4 --output plan.json
```

### 10.3 有字幕区域：清理后重新构图

```bash
python3 "$PIPELINE" plan INPUT \
  --remove-region '5%:72%:90%:22%' \
  --confirm-authorized-removal \
  --composition fit --resolution 1080x1920 \
  --approve-preview --approve-high-risk \
  --final-output OUTPUT.mp4 --output plan.json
```

### 10.4 快节奏短视频：裁尾 + 1.06× + 轻滤镜

```bash
python3 "$PIPELINE" plan INPUT \
  --analysis analysis.json --accept-suggested-tail \
  --speed 1.06 --filter cinematic \
  --approve-preview \
  --final-output OUTPUT.mp4 --output plan.json
```

## 12. 审批参数速查

| 参数 | 作用 |
|---|---|
| `--accept-suggested-tail` | 接受分析报告中的 `junk_tail` 建议 |
| `--approve-preview` | 确认已查看预览并接受中风险视觉变化 |
| `--approve-conflicts` | 接受 `color + filter` 等组合冲突 |
| `--approve-high-risk` | 接受 `stretch`、垂直/双向反转、大区域/多区域清理 |
| `--confirm-authorized-removal` | 确认指定水印/硬字幕区域有编辑授权 |

这些参数不是“强制执行开关”，而是写入 JSON plan 的 review gate。输入哈希、plan hash 或输出验证失败时仍会拒绝执行。

## 13. 常见错误

### `ANALYSIS_REQUIRED`

使用了 `--accept-suggested-tail`，但没有提供 `--analysis analysis.json`。

### `REMOVAL_AUTHORIZATION_REQUIRED`

使用 `--remove-region` 或 `--subtitle-band` 时缺少 `--confirm-authorized-removal`。

### `PREVIEW_NOT_APPROVED`

启用了镜像、构图、增强或区域清理，但没有 `--approve-preview`，或计划的 `preview.status` 仍为 `pending`。

### `CONFLICT_NOT_APPROVED`

同时使用 `--color` 和 `--filter`，但缺少 `--approve-conflicts`。

### `HIGH_RISK_NOT_APPROVED`

使用了 `stretch`、`--flip vertical/both` 或大区域清理，但缺少 `--approve-high-risk`。

### `INPUT_HASH_MISMATCH`

生成 plan 后源视频发生变化。请重新运行 `analyze` 和 `plan`，不要复用旧计划。

### `OUTPUT_EXISTS`

目标输出已存在。换一个 `--final-output` 路径；Skill 默认不覆盖文件。

## 14. 当前能力边界

已支持：媒体探测、精确哈希与基础感知指纹去重、相似候选组和置信度报告、非内容元数据规范化、多证据废片尾分析、文字标签/Logo 二维候选定位与最小损失裁剪建议、显式裁切、固定区域 `delogo`、`fit/fill/stretch`、镜像反转、预设调色/滤镜、降噪、锐化、音画同步变速、临时文件验证和无覆盖交付。

暂未内置：`smart/manual` 主体构图、移动水印跟踪、OCR 字幕重建、VSR 模型和批处理续跑。遇到这些请求时应先生成计划或报告，不要假装已经执行。

## 15. 最常用的一句话调用

下面的句子可以直接复制到 Codex 对话框；把方括号内容替换成实际路径或参数即可。默认先生成报告/计划，不会覆盖源文件。

### A. 只分析与查重

1. `使用 $super-video-mix 分析 [视频路径] 的媒体信息和废片尾，不要修改视频，输出中文结论和 analysis.json。`
2. `使用 $super-video-mix 按 douyin 预设分析 [视频路径]，重点判断片尾黑屏、静音、静帧和空镜，只给清理建议，不执行。`
3. `使用 $super-video-mix 检查 [文件夹路径] 中完全相同的视频，生成 duplicates.json，不删除任何文件。`
4. `使用 $super-video-mix 只探测 [视频路径] 的编码、分辨率、帧率、音轨和时长，不扫描废片尾。`

### B. 清理片尾

5. `使用 $super-video-mix 分析并清理 [视频路径] 的无效片尾，先给我看 junk_tail 建议，确认后输出到 [输出路径]。`
6. `使用 $super-video-mix 清理 [视频路径] 后约 [秒数] 的黑屏静音片尾，保留源文件，生成 plan、execution 和 verification 报告。`
7. `使用 $super-video-mix 按 tiktok 预设分析 [视频路径]，只有黑屏和静音等多证据成立时才建议裁尾，不要固定减去几秒。`

### C. 构图与镜像

8. `使用 $super-video-mix 将授权视频 [视频路径] 等比 fit 到 1080x1920，完整保留画面并补边，不要拉伸，输出到 [输出路径]。`
9. `使用 $super-video-mix 将授权视频 [视频路径] fill 到 1080x1920，允许裁掉边缘但不要裁掉主体，先生成预览计划。`
10. `使用 $super-video-mix 对 [视频路径] 做水平镜像，先生成需要预览的 plan，确认后输出到 [输出路径]。`
11. `使用 $super-video-mix 对 [视频路径] 做上下反转，标记 high risk，要求 preview 和明确审批后再输出到 [输出路径]。`
12. `使用 $super-video-mix 将 [视频路径] 双向反转，也就是 hflip 加 vflip，禁止把它当作去重手段，先生成审批计划。`

### D. 视觉增强

13. `使用 $super-video-mix 给 [视频路径] 使用 natural 调色，其他增强关闭，先生成 plan 和预览要求。`
14. `使用 $super-video-mix 给 [视频路径] 加 cinematic 滤镜并保持分辨率和帧率不变，先检查视觉风险再执行。`
15. `使用 $super-video-mix 对 [视频路径] 做 light 降噪后再做 light 锐化，不要镜像、裁切或变速，输出到 [输出路径]。`
16. `使用 $super-video-mix 对 [视频路径] 做 warm 调色和 cinematic 滤镜，明确报告 color/filter 冲突并等待我批准后执行。`
17. `使用 $super-video-mix 对 [视频路径] 做 medium 降噪和 medium 锐化，先生成预览计划，不要直接渲染。`

### E. 音画同步变速

18. `使用 $super-video-mix 将 [视频路径] 加速到 1.06 倍，视频和音频必须同步变速，执行后检查 av_sync_duration。`
19. `使用 $super-video-mix 将 [视频路径] 慢放到 0.8 倍，保留音轨并验证音视频时长差，不要使用 -shortest 掩盖问题。`
20. `使用 $super-video-mix 对 [视频路径] 先清理建议片尾，再做 1.06 倍同步变速，先给我 plan，不要覆盖源文件。`

### F. 固定水印或硬字幕区域

21. `使用 $super-video-mix 清理我有授权的 [视频路径] 右下角固定区域 78%:78%:20%:18%，要求确认授权、预览和新输出。`
22. `使用 $super-video-mix 清理我有授权的 [视频路径] 底部硬字幕带 0.72:0.94，使用保守 delogo，先生成 plan。`
23. `使用 $super-video-mix 清理 [视频路径] 的两个固定授权区域 [区域1] 和 [区域2]，标记 high risk，不要处理移动水印。`
24. `使用 $super-video-mix 判断 [视频路径] 的字幕是否适合固定区域 delogo；如果是运动背景或大面积区域，只输出 VSR 路由建议，不要强行清理。`

### G. 常用完整组合

25. `使用 $super-video-mix 处理授权抖音视频 [视频路径]：清理废片尾、fit 到 1080x1920、light 降噪、light 锐化，先生成 plan 和预览要求。`
26. `使用 $super-video-mix 处理授权横屏视频 [视频路径]：fit 到 1080x1920、水平镜像、natural 调色，报告每项风险后再执行到 [输出路径]。`
27. `使用 $super-video-mix 处理授权视频 [视频路径]：清理底部硬字幕区域、fill 到 1080x1920、加 soft 滤镜并做 1.06 倍同步变速，所有高风险步骤先审批。`
28. `使用 $super-video-mix 对 [视频文件夹] 先做精确查重，再逐个生成清理和翻新计划，跳过重复素材，不删除源文件。`

### 一句话写法公式

最稳定的调用通常包含四部分：

`Skill 名 + 输入对象 + 目标操作/组合 + 输出与审批要求`

例如：

`使用 $super-video-mix 处理授权视频 [INPUT]：清理建议片尾、fit 到 1080x1920、水平镜像和 light 降噪；先生成 plan，预览确认后输出到 [OUTPUT]，不要覆盖源文件。`
